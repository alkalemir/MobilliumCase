import Foundation


enum Views: String {
    case movieList = "movieListVC"
    case login = "loginVC"
}

enum Cells: String {
    case movieCell = "MovieCell"
}

enum StoryBoards: String {
    case main = "Main"
}

enum Images: String {
    case checkBoxImage = "checkmark.square.fill"
}
